rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // LexiClear AI enforces zero-knowledge ephemeral processing.
    // Persistent document storage is disallowed to protect user confidentiality.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
